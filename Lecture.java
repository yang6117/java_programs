// Lecture.javax

public class Lecture{
/*
  public static double averageArray(int[] a ){
    double average = 0;
    double sum = 0;
    int row;
    int col;
    int[][] array = new int[4][4];

    for(row = 0; row < array.length; row++){ // from fifteen.java
      for(col = 0; col < array[row].length; col++){
        array[row][col] = row * array[row].length + col + 1;
      }
      array[array.length - 1][array[array.length - 1].length - 1] = 0;
    }

    for(row = 0; row < array.length; row++){
      for

    }


    for(int index = 0; index < a.length; index++){
      sum = sum + a.[index];
    }
    average = sum/a.length;
    return average;

  } */
  public static double findMax(double a, double b, double c){

  }
  public static int findmaxArray(int[] a){

  }
}
